---
apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-funcx-ws
spec:
  ports:
   - port: 6000
     targetPort: 6000
     name: "tcp"
     protocol: TCP
  selector:
    app: {{ .Release.Name }}-funcx-ws
  type: ClusterIP